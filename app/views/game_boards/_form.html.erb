<%= form_for(@game_board) do |f| %>
  <% if @game_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_board.errors.count, "error") %> prohibited this game_board from being saved:</h2>

      <ul>
      <% @game_board.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
